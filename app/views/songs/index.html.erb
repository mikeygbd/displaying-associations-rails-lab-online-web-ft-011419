<h1>Songs</h1>
<div>
  <% @songs.each do |song| %>
  <ul>
    <li><a href="<%= song_path(song) %>">
      <%= song.artist_name %> - <%= song.title %><br>
      </a>
    </li>
    <% end %>
  </ul>

</div>
